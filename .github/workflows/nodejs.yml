name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: yarn
    - run: yarn run lint
    - run: yarn run validate
    - run: yarn run build
      env:
        CI: true
